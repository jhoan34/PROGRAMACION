// crea valores unicos para un tipo de dato

const id = Symbol("id");            // Identificador de "id"
const unique = Symbol("unique");    // Identificador de "unique"

Symbol("unique") === Symbol("unique");  // false
